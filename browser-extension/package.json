{
  "name": "pwmgr-browser-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Companion browser extension for PwMgr — autofills credentials from the desktop vault.",
  "scripts": {
    "build": "vite build",
    "watch": "vite build --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
